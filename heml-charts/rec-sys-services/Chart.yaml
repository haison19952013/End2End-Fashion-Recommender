apiVersion: v1
name: rec-sys-services
description: My Helm Chart for the fashion recommendation system services

# A chart can be `application` or `library`,
# we don't use `library` so often
type: application

# The chart vesion, which should be changed every time
# you make an update to the chart
version: 0.0.0

# The version number of the application being deployed
appVersion: "0.0.0"

maintainers:
  - email: 'haison19952013@gmail.com'
    name: sonhaile
